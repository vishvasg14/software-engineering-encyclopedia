# 05 — Classes and Inheritance (ES2022+)

Demonstrates ES6 classes with public/private fields and overrides.

## Run

```bash
# .js version (plain Node 22+):
node classes.js

# .ts version (requires TS):
npx tsx classes.ts
```