# 10 — TypeScript Narrowing and Control Flow

Demonstrates `typeof`, `instanceof`, discriminated unions, and control-flow analysis.

## Run

```bash
npx tsx narrowing.ts
# or: npx tsc narrowing.ts && node narrowing.js
```