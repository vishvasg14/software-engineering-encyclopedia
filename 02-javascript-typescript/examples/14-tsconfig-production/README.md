# 14 — Production tsconfig

Demonstrates a strict, modern tsconfig for production.

## Files

- `tsconfig.json` — strict, modern TypeScript config.
- `app.ts` — sample code showing how strict checks work.

## Run

```bash
npx tsc --noEmit
# or compile:
npx tsc
node dist/app.js
```