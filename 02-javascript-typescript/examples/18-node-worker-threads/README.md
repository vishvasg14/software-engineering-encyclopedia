# 18 — Node.js Worker Threads

Demonstrates CPU-bound work parallelization using `worker_threads`.

## Run

```bash
node worker-threads.js
```

Expected: both workers compute concurrently; main thread receives results.